Added
^^^^^

* Added a ``physics_args`` block (backend, solver variant, adaptive settings) to the episode
  metadata written by :class:`~isaaclab.managers.RecorderManager`, so recorded datasets are
  traceable to the physics configuration that produced them.
